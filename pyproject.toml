[project]
name = "consolidar-arquivos"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "chardet>=5.2.0",
    "pathspec>=0.12",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
